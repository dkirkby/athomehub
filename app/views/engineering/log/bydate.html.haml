%h1 Engineering::DeviceLog#bydate

%p Displaying #{@logs.length} device log messages received during the #{@ival}s from #{@begin_at.localtime} to #{@end_at.localtime} with severity at least #{@level}.

=render :partial => "logs"
