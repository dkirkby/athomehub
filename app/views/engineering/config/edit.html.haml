%h1 Update Device Configuration

-form_for(:device_config,@config,:url=>{:action=>"update",:id=>@config}) do |@form|
	= @form.error_messages
	=render :partial => "form"
	%p
		= @form.submit 'Update'

= link_to 'Back', :action => :index