-content_for :head do
	=javascript_include_tag 'excanvas','flot','eng-plot'

%h1 Device Samples Recorded in a Time Window

%p Displaying #{@samples.length} samples received during the #{@ival}s from #{@begin_at.localtime} to #{@end_at.localtime}.

=render :partial => "config" if @config
=render :partial => "plots"
