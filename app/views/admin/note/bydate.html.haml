%h1 Admin::Note#bydate

-if flash[:notice]
	#error_msg= flash[:notice]

%p Displaying #{@notes.length} notes created during the #{@ival}s from #{@begin_at.localtime} to #{@end_at.localtime}.

=render :partial => "notes"
