%h1 Admin::Note#index

-if flash[:notice]
	#error_msg= flash[:notice]

Displaying #{@notes.length} notes.

%table
	%tr
		%th ID
		%th When
		%th Who
		%th What

	-@notes.each do |n|
		%tr
			%td&=n.id
			%td&="#{time_ago_in_words n.created_at} ago"
			%td&=n.user.name if n.user
			%td&=truncate n.body
			%td=link_to 'Show', :action=>:show, :id => n
			%td=link_to 'Delete', {:action=>:destroy, :id=>n}, options=:confirm => 'Are you sure?'
